# Second-pass fixture note

The request template is produced by the corrected deterministic builder. Real second-pass model output is intentionally not fabricated in this package because the remote-sensing images are not included. On AutoDL/HTTP runtime, the same builder uses uploaded/local Agent1 artifacts and then Agent3-V5.2 executes the request.
